// Menggunakan kutip tunggal
let namaDepan = 'Andi';

// Menggunakan kutip ganda
let namaBelakang = "Wijaya";

// Menggunakan template literal (backtick)
let namaLengkap = `${namaDepan} ${namaBelakang}`;

// String kosong
let kosong = "";

// Menggabungkan string (concatenation)
let sapaan = "Halo, " + namaDepan + "!";

// Mengakses karakter dalam string
let hurufPertama = namaDepan[0]; // 'A'

// Menghitung panjang string
let panjangNama = namaLengkap.length;

// Menggunakan method string
let hurufBesar = namaDepan.toUpperCase(); // 'ANDI'
let hurufKecil = namaBelakang.toLowerCase(); //
